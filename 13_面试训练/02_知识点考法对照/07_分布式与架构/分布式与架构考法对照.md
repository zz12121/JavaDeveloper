# 分布式与架构 - 知识点考法对照

## CAP 定理

**第一问**：「CAP 是什么？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 为什么 P（分区容错）必须满足？ | 网络分区不可避免 |
| 第二层 | CP 系统和 AP 系统举例？ | ZooKeeper（CP）/ Eureka（AP）|
| 第三层 | BASE 理论和 CAP 的关系？ | BASE 是 AP 的延伸 |

**对比型考法**：「ZooKeeper 是 CP 还是 AP？为什么？」

---

## 分布式锁

**第一问**：「分布式锁怎么实现？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | Redis 分布式锁 SETNX + TTL 够吗？ | 原子性 + 续期 |
| 第二层 | Redisson 的看门狗机制是什么？ | 自动续期 |
| 第三层 | RedLock 为什么有争议？ | 时钟漂移问题 |

**实战型考法**：「你在项目里用过分布式锁吗？用的什么方案？」

---

## 分布式事务

**第一问**：「分布式事务有哪些解决方案？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 2PC 有什么缺陷？ | 协调者单点 / 数据不一致 |
| 第二层 | TCC 和 Saga 的区别？ | 资源预留 vs 补偿 |
| 第三层 | 本地消息表和 RocketMQ 事务消息的区别？ | 实现复杂度 |

---

## 限流

**第一问**：「常见的限流算法有哪些？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 令牌桶和漏桶的区别？ | 允许突发 vs 完全平滑 |
| 第二层 | 固定窗口的「临界双倍问题」具体是什么？ | 窗口边界附近 |
| 第三层 | 分布式限流怎么实现？ | Redis + Lua 脚本 |

**实战型考法**：「你的系统做过限流吗？阈值怎么设的？」

---

## 幂等性

**第一问**：「如何设计幂等？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 数据库唯一索引怎么用？ | 防重复插入 |
| 第二层 | Token 机制怎么实现幂等？ | 防重复提交 |
| 第三层 | 消息队列重复消费怎么处理？ | 消费者幂等 |

---

## 熔断与降级

**第一问**：「熔断和降级有什么区别？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 熔断的三个状态是什么？ | CLOSED / OPEN / HALF_OPEN |
| 第二层 | Sentinel 和 Hystrix 的区别？ | 线程隔离 vs 信号量 |
| 第三层 | 降级返回什么数据？ | 缓存数据 / 默认值 / 友好提示 |

---

## 服务注册发现

**第一问**：「服务注册发现是怎么实现的？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 客户端发现和服务端发现的区别？ | Eureka vs Nginx |
| 第二层 | Eureka 的自我保护机制是什么？ | 防止误杀 |
| 第三层 | Nacos 为什么能同时 CP/AP？ | Raft + 心跳 |

---

## 负载均衡

**第一问**：「常见的负载均衡算法有哪些？」

| 追问层次 | 追问 | 考点 |
|---------|------|------|
| 第一层 | 一致性哈希相比普通哈希的优势？ | 节点变更影响范围小 |
| 第二层 | Ribbon 和 Nginx 的负载均衡有什么区别？ | 客户端 vs 服务端 |
| 第三层 | 灰度发布用什么负载均衡策略？ | 七层（按 Header/权重）|
