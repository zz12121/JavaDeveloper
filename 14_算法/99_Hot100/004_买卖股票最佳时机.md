# #121 买卖股票的最佳时机

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | #121 |
| 链接 | https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/ |
| 难度 | 🟢 简单 |
| 类型 | 数组 / 贪心 |

## 题干

给定一个数组 `prices`，它的第 `i` 个元素是一支给定股票第 `i` 天的价格。

如果你最多只允许完成**一笔交易**（即买入和卖出一支股票一次），请计算你能获取的最大利润。

注意：你不能在买入股票前卖出股票。

### 示例

```
输入：prices = [7,1,5,3,6,4]
输出：5
解释：在第 2 天（股票价格 = 1）买入，在第 5 天（股票价格 = 6）卖出，最大利润 = 6-1 = 5
```

---

*状态：⬜ 未做*

---

我的解题答案：
```java
private static int getMaxDiff(int[] a) {  
    int minPrice = a [0];  
    int maxDiff = 0;  
    for (int i=1; i< a.length; i++) {  
        minPrice = Math.min(minPrice, a[i]);  
        maxDiff = Math.max(maxDiff, a[i] - minPrice);  
    }  
    return maxDiff;  
}
```
1.在没有看正确答案之前，我的想法是遍历获取后一个值的差值，然后比较得到最大的值，然后后续的每个数都这么做，这是双重for循环的暴力解法。
2.看了分析之后，发现只需要再定义2个单独字段，保证最小值，然后获取和最小值的差值最大。解法很简单，但要想到这种解法。

